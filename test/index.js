"use strict";

import React from 'react';
import { shallow } from 'enzyme';
import assert from 'assert';

describe('app', () => {
  it('1 + 1 should equls to 2' , () => {
    assert(1+1 === 2);
  });
});
